import { artifactIds, ruleId } from "@/lib/stub/ids";

/** Targets for the Technical nav item's Artifacts/Rules submenu (no list route exists — UX_SPEC route table). */
export const DEFAULT_ARTIFACT_ID = artifactIds.informal;
export const DEFAULT_RULE_ID = ruleId.id;
