# Agent Run: OIW-906

## State
NEEDS_REVIEW

## Objective
Make the public repository work harder as the front door for Kaamchor's AI
consultancy funnel, per owner request of 2026-09-12: richer README (visual
demonstration per PRD §37), public-repo best-practice community files, GitHub
repo metadata, and the firm's identity/contact channels wired into the demo
funnel (landing page, `/adapt`, README). Also fixes one product bug found
during the pass.

## Changed files
- `README.md` — badge row; "What it looks like" screenshot section (PRD §37
  "animated or short visual demonstration"); "Who builds this" Kaamchor
  section with contact channels; `/adapt` limitation note now points to the
  direct-contact section.
- `docs/screenshots/*.png` (new, 7 files) — real screens captured from the
  running demo at 1440×900 during a full guided-tour pass.
- `CODE_OF_CONDUCT.md` (new, root) — Contributor Covenant 2.1 adaptation;
  enforcement contact hello@bekaamchor.com.
- `SUPPORT.md` (new, root) — support routing: docs → issues → private
  security disclosure → commercial contact.
- `.github/ISSUE_TEMPLATE/config.yml` (new) — disables blank issues; contact
  links for commercial enquiries and private security disclosure.
- `apps/web/src/lib/firm.ts` (new) — single source for firm name/site/email/
  WhatsApp/LinkedIn.
- `apps/web/src/app/page.tsx` — landing footer gains "Built by Kaamchor —
  hello@bekaamchor.com".
- `apps/web/src/app/adapt/page.tsx` — direct-contact panel (email + WhatsApp)
  above the assessment form.
- `apps/web/src/app/adapt/AdaptForm.tsx` — success state adds the direct
  email line ("it comes straight to the team that built this").
- `apps/web/src/app/w/[workspace]/decisions/DecisionCard.tsx` — bug fix:
  evidence-link `<span key={link.href}>` collided when a decision cites the
  same artifact twice (React duplicate-key console error, observed 3× during
  the guided tour's decision step). Key now `${link.href}-${index}`.

## Repo settings changed outside git (via `gh`, owner account)
- Description set (platform summary + "Built by Kaamchor").
- Homepage URL set to https://bekaamchor.com.
- Topics added: ai, ai-agents, human-in-the-loop, workflow-automation,
  operations, audit-trail, evidence, nextjs, typescript, postgres, drizzle,
  pnpm, scenario-packs, open-source.

## Commits
See the branch `agent/zcode/OIW-906-public-demo-funnel` (hashes filled in the
PR body; this file written before commit).

## Tests run and results
- `pnpm lint` — pass.
- `pnpm typecheck` — pass.
- `pnpm validate:packs` — pass (3 loaded + template; two pre-existing
  tolerated fixture warnings unchanged).
- `pnpm architecture:check` — pass (neutrality, boundaries, credentials).
- `pnpm test` — 60/60 pass; `apps/web` `pnpm test` — 132/132 pass.
- `pnpm build` — pass (pre-existing Turbopack file-tracing warning from
  `scenario-sdk/dist/fixtures.js`, unchanged by this task).
- `pnpm eval` — 1.000 on every dimension, all packs.
- `cd apps/web && pnpm test:e2e` — 23/23 pass (after a one-time local
  `pnpm exec playwright install chromium`; first local run failed with
  "Executable doesn't exist", an environment issue, not a product one).

## Manual GUI verification (browser, 1440×900)
Full visitor journey executed black-box: landing → scenario selector →
guided tour steps 1–10 (process artifact → Needs review → review accept →
rule trace → case → decision approval with required comment → leadership
dashboard updates → audit trail → final CTA) → `/adapt` empty-submit
validation (6 field errors) → successful submit → row verified in
`assessment_submissions` and 12 analytics event types in `analytics_events`.
Screenshots in `docs/screenshots/` are from this session. Known dev-only
observations: Playwright locator clicks intermittently time out on this
machine while real-mouse clicks succeed (hydration timing on a slow
filesystem; CI is green); a transient blank page occurred once during a dev
compile of a route not yet visited.

## Acceptance criteria status
No formal task packet existed (owner-requested improvement pass); criteria
drawn from the owner's request:
- Firm identifiable in-repo with working contact channels — pass (README
  section, landing footer, `/adapt` panel and success state).
- Visual demonstration in README — pass (6 real screenshots; PRD §37 item).
- Public-repo best-practice files — pass (CoC, SUPPORT, issue-template
  config; CONTRIBUTING already existed in `docs/` and is linked from the
  badges row).
- Repo metadata (description/homepage/topics) — pass (live on GitHub).
- No product-rule violations — pass (`architecture:check` green; firm
  attribution is platform ownership, not industry domain logic; UI text is
  firm-level, not pack-level).
- Existing copy asserted by tests unchanged — pass
  ("Thanks — we'll follow up shortly." preserved; e2e/unit suites green).

## Architectural decisions
- Firm identity lives in one module (`apps/web/src/lib/firm.ts`) rather than
  being inlined, so a future rename or channel change is a one-file edit.
- Contact channels are presented as *additional* paths, not replacements:
  the Postgres-backed `/adapt` form remains the PRD §23.2 qualified CTA;
  email/WhatsApp are surfaced because the email/webhook sinks are still
  interfaces-only (SESSION.md debt) and a prospect must never hit a dead end.
- Screenshots live in `docs/screenshots/` (committed) rather than a CDN or
  release asset so the README renders offline and in forks.

## Assumptions and deviations
- Task ID OIW-906 chosen as the next free number after OIW-905; no PRD
  backlog entry existed for "public funnel enrichment" — this run treats the
  owner's chat request as the packet.
- WhatsApp number taken from the firm site's `wa.me/918404029692` link;
  display-formatted as "+91 84040 29692".
- Kaamchor's own assessment funnel ("16 questions, 7 minutes") was referenced
  in the README only as the firm link, not embedded — keeping OIW's own CTA
  primary per PRD §23.2.
- GitHub Discussions was left disabled (owner decision not requested);
  `SUPPORT.md` routes questions to Issues and email instead.

## Dependency requests
None.

## Known limitations / follow-up
- `/adapt` submissions still land in Postgres only (no notification path);
  the direct-contact channels mitigate but do not replace the OIW-904 sink
  decision.
- Screenshots will drift as the UI evolves; consider a `pnpm demo:shots`
  script that replays the tour headlessly and regenerates `docs/screenshots/`
  (the e2e suite already contains a "screenshots of changed screens" spec
  that could seed it).
- The guided-tour dialog appears in two committed screenshots (review queue,
  decision card); accepted as authentic documentation of the tour.
- Landing page remains visually sparse at 1440×900 (large empty lower half);
  a designed hero (screenshot or diagram) would lift it — left out to keep
  this change reviewable.
