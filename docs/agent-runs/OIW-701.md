# Agent Run: OIW-701

## State
COMPLETE

## Changed files

- `packages/application/test/common-lifecycle.integration.test.ts` — one
  registry-parameterized lifecycle proof for every loaded pack, plus the two
  new packs' complete primary-storyline approval tests.
- `scenario-packs/process-exceptions/` — minimal extraction, gold, schema,
  event-order, rule, case-trigger, seed and README corrections found by the
  lifecycle proof.
- `scenario-packs/document-assurance/` — minimal extraction, gold, schema,
  event-order, rule, seed and README corrections found by the lifecycle proof.
- `docs/agent-runs/OIW-701.md` — this COMPLETE handoff, replacing the earlier
  BLOCKED handoff.

No file under `scenario-packs/asset-reliability/`, no fixture Artifact, and no
engine, contract, migration, app, task packet, lockfile or `SESSION.md` file was
changed.

## Commits

- `43a1213` — original BLOCKED handoff.
- `0600974` — expanded BLOCKED evidence and OIW-703 follow-up.
- `76b7381` — merged OIW-703 from `main`, resolving the ADR-010 blocker.
- The implementation/COMPLETE-handoff commit is the commit containing this
  file; its hash is intentionally not embedded in itself.

## Tests run and results

- `docker compose up -d && pnpm db:migrate` — PASS; PostgreSQL running and
  migrations applied.
- `pnpm lint` — PASS, zero warnings.
- `pnpm typecheck` — PASS for tools, all workspace packages and the web app.
- `pnpm test` — PASS. Core: 30 files / 192 tests, including the new 11-test
  common lifecycle suite. Web: 15 files / 103 tests.
- `pnpm build` — PASS. The pre-existing non-fatal Turbopack dynamic-fixture-
  filesystem warning remains.
- `pnpm validate:packs` — PASS: 3 loaded, 0 invalid, 0 skipped.
- `pnpm architecture:check` — PASS: core packages remain neutral and imports
  respect pack boundaries.
- `pnpm eval` — PASS: command is wired; runner/datasets remain assigned
  follow-up work.
- Duplicate extraction equality: `diff -u` between Process Exceptions
  smoke-001 and edge-005, and between Document Assurance demo-005 and edge-005
  — PASS with no differences.
- Frozen Artifact integrity:
  `find scenario-packs -path '*/fixtures/*/artifacts/*' -type f -print0 | sort -z | xargs -0 sha256sum | sha256sum`
  — `6bcfbd0c22fa9a45b8a5f13b96e56dc275c1efaf576c029fe1400e3d26c0e673`,
  unchanged from OIW-110 and the BLOCKED phase.
- `git diff --check` — PASS.

## Acceptance criteria status

- **Common lifecycle test green for all three packs — PASS.** It builds the
  registry from `scenario-packs/`, requires exactly the three valid registered
  packs, seeds an isolated workspace per run, processes every smoke fixture,
  verifies fixture-provider and persisted full-shape extraction parity, then
  asserts Event, Entity, Signal and Case gold. Each pack's configured demo
  reaches a high/critical Decision awaiting human approval, has no Approval,
  and rejects direct status bypass.
- **Duplicate and abstention behavior — PASS.** Every pack's checksum-identical
  edge resubmission is deduplicated and produces a duplicate-suppression audit;
  every edge gold entry containing an insufficient-evidence Observation is
  processed and retains null value, pending review and insufficient evidence.
  No edge path creates an Approval.
- **Process Exceptions primary storyline — PASS.** All 12 KM-LOT-448 demo
  fixtures reach the high-risk `hold-affected-output` Decision in
  `awaiting-approval`; `ApprovalService.apply` records the supervisor/QC human
  Approval and moves it to `approved`.
- **Document Assurance primary storyline — PASS.** All 12 Project Falcon demo
  fixtures reach the high-risk `accept-exception` Decision in
  `awaiting-approval`; the authorised-reviewer path records the human Approval
  and moves it to `approved`.
- **Asset Reliability unchanged — PASS.** It passes the same common test with
  no pack diff.
- **Wired into `pnpm test` and all local gates green — PASS.** The root Vitest
  glob automatically includes the new `packages/application/test/*.test.ts`
  file.

## Pack-data fixes and narrative justification

### Process Exceptions

- Made edge-005's extraction file exactly equal to smoke-001's extraction,
  including Tomas Reyes's normalised name. The frozen source is byte-identical,
  so different extraction semantics were invalid.
- Reordered Event definitions from specific operational evidence to general
  reporting fallbacks and tightened overlapping definitions. Added explicit
  `process-stage` Observations to demo-001/005/008/010; production summaries
  now require an allowed production stage, process deviations require stage,
  held-output aging requires hold scope, and inventory events require the
  disposition fact. This preserves the narrative's distinctions between
  production summaries, in-process deviations, shift reports, inventory notes
  and routine handovers.
- Added the `investigation-request` Observation schema and demo-004 extraction;
  the root-cause-requested Event now requires the source's explicit request
  instead of inferring it from ordinary batch references.
- Added missing synthetic seed batches B-2098, B-2099, B-2109 and B-2115,
  corrected smoke/demo entity gold, and normalised the two-batch routine report
  to its authored primary B-2109 link. This makes Event primary-entity linkage
  match the frozen sources without inventing core behavior.
- Widened `expected-value` honestly from numeric-only to string and preserved
  numeric targets with units (`cP`, `g`, `%`). This admits edge-007's genuine
  textual inspection criterion, `pass - no particulate`, without routing
  supported evidence to structural review or losing units in JSON persistence.
- Expanded `disposition-status` to the narrative's actual current states and
  normalised smoke-008's explicit released result to `released` while retaining
  the full source excerpt as evidence.
- Corrected rule predicates to facts the runtime can evaluate. Demo-006's
  explicit cross-batch/supplier-cause record raises the repeated and supplier
  pattern signals; demo-007's held-output aging event raises backlog only when
  it reaches an open related case; demo-004 records both the narrative's
  unverified root-cause hypothesis signal and the investigation action.
- Allowed the formal-hold approval rule to trigger an exception case and
  corrected demo-009 gold to include its resolved KM-LOT-448 primary entity.
  The narrative makes that formal supplier-lot hold the origin of the primary
  Decision, so it must have a related Case rather than an orphan Decision.

### Document Assurance

- Made edge-005's extraction exactly equal to demo-005's, restoring the omitted
  obligation Observation. Byte-identical source content must have identical
  extraction meaning.
- Corrected demo-001 and smoke-007 evidence excerpts to the exact frozen source
  line breaks and added smoke-007's explicit effective date. Evidence remains
  source-verifiable instead of relying on text not present in the Artifact.
- Added the `amendment-reference` Observation/schema to demo-008 and made
  `contract-amended` require it. This distinguishes the legitimate Project
  Falcon amendment from initial contract execution while keeping the parent
  agreement link.
- Reconciled value-discriminated and overlapping Event definitions: conflict
  identification precedes generic clarification, obligation status uses its
  governing document link, and exception proposal/approval require the exact
  `proposed`/`approved` values before the cross-engagement correction fallback.
  These are the narrative-authored meanings now supported by OIW-703/ADR-010.
- Added source-backed Project Falcon document references to demo-006/007 and
  demo-010/011/012, and corrected demo-009 to link the parent MSA rather than
  the amendment row. Updated gold and the CFU service-order alias accordingly,
  so the operational Events link the documents explicitly named by each
  frozen source.
- Corrected the missed-deadline aggregate threshold from two prior related
  reports to one prior report: the current Q2 report supplies the still-missing
  evidence fact, while `related-event-count` intentionally excludes the current
  Event. This implements the narrative's “second consecutive report” rule.
- Corrected smoke entity gold where the routine sign-off explicitly names the
  NDA/service-order record selected by the Event's primary reference.

Both READMEs now explain the clarified Event/rule/Observation semantics.

## Architectural decisions

No new ADR is required. The test is entirely registry-driven and contains no
pack-ID conditional in core behavior. Pack-specific semantics remain in pack
schemas, rules, fixtures, gold and labels. OIW-703's neutral implementation of
ADR-010 is consumed as merged; no implementation package was modified.

The demo signal assertion checks that each authored rule fires within the
processed lifecycle rather than requiring aggregate signals to be timestamped
on the gold fixture that conceptually originated them. Smoke signals retain
strict per-Event linkage assertions.

## Assumptions and deviations

- `scenario-packs/*/narrative/expected-outcomes.md` was treated as read-only
  semantic truth.
- The earlier BLOCKED phase is retained here as a deviation record: OIW-701
  originally stopped because Event assembly ignored `requiredObservationValues`.
  OIW-703 fixed that engine gap under its own owned paths and was merged into
  this branch before implementation resumed.
- The common Decision proof processes each pack's configured demo until its
  first human-gated Decision; the two new packs additionally have dedicated
  tests that process all 12 primary-storyline fixtures before applying the
  recorded human Approval. This avoids extending Asset Reliability beyond its
  already-proven primary decision while still using one neutral common path.
- No Playwright run was required because this task has no UI or workflow-view
  change.

## Dependency requests

None. No package or lockfile change was needed.

## Known limitations / follow-up

- The existing Next/Turbopack dynamic fixture-filesystem tracing warning is
  unchanged and non-fatal.
- Guided tours remain OIW-702 scope.

---
COMPLETE
