# Agent Run: OIW-002

## State

COMPLETE

## Changed files

- `packages/contracts/src/extraction.ts` — added the `negated` Proposed
  Observation variant and optional alternative candidates on extracted values.
- `packages/contracts/src/domain.ts` — added persisted alternative candidates,
  `negated` evidence status and `conflicting` review status.
- `packages/contracts/src/extraction.test.ts` and `domain.test.ts` — added eight
  valid and invalid contract tests for the three amendments.
- `docs/DOMAIN_MODEL.md` — documented the version 1.1 Observation semantics.
- `docs/decisions/ADR-008-negative-findings-and-candidates.md` — recorded the
  amendments and the deliberate near-duplicate/source-quality deferrals.
- `docs/agent-runs/OIW-002.md` — this handoff.

## Commits

- `947b0eb` — `OIW-002: amend observation contracts`
- Handoff commit — the commit containing this agent-run file.

## Tests run and results

Initial installation and baseline, passed:

```text
pnpm install
Scope: all 13 workspace projects
Already up to date
Done in 68ms using pnpm v11.25.0

pnpm test
Test Files  3 passed (3)
Tests  38 passed (38)
```

Focused amended-contract checks, passed:

```text
pnpm test:contracts && pnpm lint && pnpm typecheck
Test Files  3 passed (3)
Tests  46 passed (46)
```

Final required command, passed:

```text
pnpm lint && pnpm typecheck && pnpm test && pnpm build && pnpm architecture:check
Test Files  3 passed (3)
Tests  46 passed (46)
Architecture checks passed: core packages are neutral and domain imports respect pack boundaries.
```

Final diff validation, passed:

```text
git diff --check
(no output)
```

No test or quality-gate failures occurred during implementation.

## Acceptance criteria status

- Pass — the five required quality commands pass in the packet's exact order.
- Pass — all 38 pre-existing tests pass without changes to their intent; eight
  additive tests bring the suite to 46 tests.
- Pass — a negated Proposed Observation requires at least one evidence item,
  confidence in the zero-to-one range and a non-empty reason.
- Pass — extracted and persisted Observations accept optional alternative
  candidates; invalid candidate confidence is rejected.
- Pass — persisted Observations accept `negated` evidence status and
  `conflicting` review status, while unknown review states remain invalid.
- Pass — ADR-008 exists and `docs/DOMAIN_MODEL.md` reflects all three changes.
- Pass — existing payloads without alternative candidates still parse; no
  existing union member or enum member was removed.

## Architectural decisions

- Kept the negated provider result structurally parallel to the existing
  insufficient-evidence result, but required source evidence and used the
  discriminant to distinguish confirmed absence from unknown information.
- Made `alternativeCandidates` optional rather than default-transforming it,
  so existing payload round trips remain unchanged while both omission and an
  explicit empty array are valid.
- ADR-008 records why near-duplicate matching and source-quality flags require
  coordinated engine design and were not added to this contract-only task.

## Assumptions and deviations

- A negated Proposed Observation uses `value: null` and
  `normalisedValue: null`; the `negated` discriminant, evidence and reason carry
  the confirmed-absence meaning without fabricating a positive value.
- No deviations from the task packet. No paths outside the owned contract,
  documentation, ADR and required handoff paths were edited. `SESSION.md` and
  `docs/tasks/` were not edited.

## Dependency requests

None. No dependency declarations or lockfile content changed.

## Known limitations / follow-up

- Near-duplicate handling remains deferred to Wave 1 entity-resolution and
  Event-assembly design.
- Source-quality flags remain deferred until Artifact, segment, ingestion and
  review semantics can be designed together.
- No engine, persistence, pack or UI implementation was included.

---

COMPLETE
