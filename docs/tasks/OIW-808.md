# Task OIW-808: Accessibility and Responsive Audit with Fixes

## Owner
Claude Code (Sonnet, second account)

## Branch
`agent/claude/OIW-808-a11y-responsive`

## Worktree
`../oiw-ux`

## Objective
Execute the Wave 4 accessibility + responsive review defined in
`docs/quality/ACCESSIBILITY.md` across every P0 screen and all three
packs' tours, fix what it finds, and pay the tracked tablet-drawer debt.
PRD OIW-808 + the "error/loading/empty states" hardening item.

## Inputs
- `AGENTS.md`, `docs/quality/ACCESSIBILITY.md` (authoritative criteria),
  `docs/UX_SPEC.md` (§5.6 tablet drawer; per-screen states), PRD §16.3,
  `apps/web/` (all screens + tour overlay), existing e2e specs

## Owned Paths
- `apps/web/` presentation: components, screens, styles, tests, e2e
  (server-side files under `apps/web/src/lib/server/` and server actions
  are OIW-810's this batch — do not edit them; BLOCKED protocol if a fix
  needs one)
- `docs/quality/ACCESSIBILITY_AUDIT.md` (new: findings + resolution table)
- `docs/agent-runs/OIW-808.md`

## Prohibited Paths
- `packages/*`, `scenario-packs/`, `apps/web/src/lib/server/**`, server
  actions, migrations, lockfile (an axe-core/Playwright accessibility
  dependency is the ONE allowed addition — add to apps/web/package.json,
  list it), `SESSION.md`, `docs/tasks/`.

## Functional Requirements
1. Automated axe scan of every P0 route (all three packs' seeded
   workspaces) integrated as a Playwright spec that fails on serious/
   critical violations; baseline documented.
2. Manual criteria pass per ACCESSIBILITY.md: keyboard-only completion of
   all three tours and of review/decision actions; focus management on
   dialogs/overlays; accessible names; no colour-only state; table
   semantics; reduced-motion respect for the tour overlay.
3. Responsive pass at tablet (768–1024) and desktop widths for every
   screen; implement the review-queue collapsible queue drawer (UX_SPEC
   §5.6, tracked debt) and fix any other overflow/stacking defects.
4. States pass: every screen's empty/loading/error states reachable and
   correct (no stub-marked remnants; error copy generic per SECURITY).
5. Findings table: each defect → severity → fix commit or justified
   deferral; screenshots before/after for visible fixes.

## Non-Goals
No visual redesign. No new features. No server changes.

## Acceptance Criteria
- axe spec green on all P0 routes for all packs; keyboard-only tour
  journeys pass for all three packs (extend e2e); tablet drawer shipped;
  `pnpm lint && typecheck && test && build` pass; full e2e green; CI green.

## Required Commands
`docker compose up -d && pnpm db:migrate && pnpm lint && pnpm typecheck && pnpm test && pnpm build && (cd apps/web && pnpm test:e2e)`

## Handoff
`docs/agent-runs/OIW-808.md` per template. PR titled
`OIW-808: accessibility and responsive audit with fixes`.
