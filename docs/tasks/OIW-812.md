# Task OIW-812: Threat-Model Remediation Sweep (Wave 4 close-out)

## Owner
Codex (high reasoning)

## Branch
`agent/codex/OIW-812-threat-remediation`

## Worktree
`../oiw-core`

## Objective
Close every remaining open item in `docs/SECURITY.md`'s threat model and
the security/quality debt tracked in `SESSION.md`, so Wave 4 exits with a
signed-off security document. PRD OIW-809/810 tail.

## Inputs
- `AGENTS.md`, `docs/SECURITY.md` (threat table + gates), `SESSION.md`
  "Tracked debt" (security observations from the OIW-810 review),
  `docs/quality/INJECTION_TEST_MATRIX.md` (gaps section, from OIW-805),
  `docs/quality/RELEASE_CHECKLIST.md`, `docs/agent-runs/OIW-810.md`,
  `OIW-811.md`, `OIW-805.md`

## Owned Paths
- `packages/application/` (tests + minimal fixes), `apps/web/src/lib/server/`
  (minimal fixes), `tests/security/`, `apps/web/e2e/` (security specs),
  `scripts/architecture-check.ts` (credential-scan breadth),
  `docs/SECURITY.md`, `docs/quality/RELEASE_CHECKLIST.md`,
  `docs/quality/INJECTION_TEST_MATRIX.md` (fill gap cells),
  `docs/DEPLOYMENT.md` (new: trusted-proxy / env requirements),
  `docs/agent-runs/OIW-812.md`; lockfile only if regeneration needed

## Prohibited Paths
- `packages/contracts/`, engines beyond minimal security fixes, SDK,
  `scenario-packs/*` content (use OIW-805's ready-made export-formula
  fixtures as-is), UI presentation, migrations, `SESSION.md`, `docs/tasks/`.

## Functional Requirements
1. Wire the injection matrix's pending export-formula cell: OIW-805's
   fixtures through OIW-811's CSV export asserting neutralisation.
2. Approval concurrency: a true-concurrency test (parallel approve/reject
   on one Decision) proving exactly one outcome persists and the other
   fails safely; fix if it doesn't hold.
3. Trusted-proxy hardening: IP extraction honours a configured trusted-
   proxy header policy (env-driven; Vercel default) instead of trusting
   x-forwarded-for unconditionally; documented in `docs/DEPLOYMENT.md`
   with all required env vars (DATABASE_URL, SESSION_SECRET(+PREVIOUS),
   rate-limit knobs, TTL) and the Supabase/Vercel setup steps.
4. Credential scan breadth: extend architecture-check patterns (generic
   high-entropy tokens, Supabase service keys, JWT-shaped strings,
   `postgres://user:pass@` URLs) with a test fixture proving detection.
5. Threat table sign-off: every SECURITY.md row has status Closed with a
   test/evidence pointer, or an explicit Accepted-risk note with owner
   rationale; release checklist updated accordingly.
6. Unauthenticated + expired-workspace export tests (noted gaps from the
   OIW-811 review).

## Non-Goals
No auth/SSO, no RLS, no infrastructure provisioning (Wave 5), no UI work.

## Acceptance Criteria
- All matrix cells filled (test or accepted-risk); SECURITY.md fully
  signed off; DEPLOYMENT.md complete; `pnpm lint && typecheck && test &&
  build && eval && architecture:check` pass; e2e green; CI green.

## Required Commands
`docker compose up -d && pnpm db:migrate && pnpm lint && pnpm typecheck && pnpm test && pnpm build && pnpm eval && pnpm architecture:check && (cd apps/web && pnpm test:e2e)`

## Handoff
`docs/agent-runs/OIW-812.md` per template. PR titled
`OIW-812: threat-model remediation sweep`.
