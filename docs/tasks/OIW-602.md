# Task OIW-602: Dashboard Wiring, Guided Tour and North-Star E2E

## Owner
Claude Code (Sonnet, m900x account)

## Branch
`agent/claude/OIW-602-dashboards-tour-e2e`

## Worktree
`../oiw-ux`

## Objective
Complete milestone M1's user-facing surface: wire the pack-configured
dashboards to real metric values, implement the guided tour for the Asset
Reliability demonstration, and write the north-star Playwright test that
walks PRD §13 end to end as a visitor. Combines PRD tasks OIW-602, OIW-206,
OIW-804.

## Background
Every engine and screen is merged. OIW-601 (metric evaluation service,
Codex) runs in parallel — consume it if merged at your branch time;
otherwise wire dashboards to repository-derived counts behind ONE clearly
marked adapter module (`lib/server/metrics.ts`) the lead swaps to the
service at reconciliation. Tour steps follow `docs/DEMO_SCRIPT.md` and
UX_SPEC; lightweight in-house overlay (no heavy dependency, packet L1).

## Inputs
- `AGENTS.md`, `docs/PLAN_AMENDMENTS.md` (A5/A7/L1-L2), `docs/UX_SPEC.md`
  (overview widgets, tour), `docs/DEMO_SCRIPT.md`, PRD §13, §20.2, FR-090,
  `scenario-packs/asset-reliability/dashboards/`, `docs/agent-runs/`
  OIW-509/506/501

## Owned Paths
- `apps/web/` only; `docs/agent-runs/OIW-602.md`

## Prohibited Paths
- Everything else; BLOCKED protocol for missing public APIs.

## Functional Requirements
1. Dashboard wiring (FR-090): leadership/operations/technical overview
   widgets render the pack's dashboard definitions with REAL values (via
   OIW-601 service or the marked adapter); all eight A5 widget types
   functional; provenance badges from metric classification (hypothetical
   always badged, never presented as measured — PRD §7.9); lens defaults
   per pack manifest.
2. Guided tour (OIW-206 scope, A7: Asset Reliability full tour only):
   in-house step overlay driven by a typed step list (target element,
   title, body, action hint); follows DEMO_SCRIPT §13 steps 1–10:
   select pack → inbox → process brake-fault message → review ambiguous
   observation → accept → event/signal → case → decision approval →
   dashboard update → technical trace. Dismissable, resumable
   (sessionStorage), keyboard accessible, focus-managed.
3. North-star Playwright test (OIW-804): a NEW visitor journey asserting
   the complete PRD §13 flow with REAL state assertions at each step:
   workspace created+seeded → artifact processed → review queue shows the
   ambiguous A-142 observation with evidence highlight → accept → event
   exists → repeat-fault signal → critical case with owner/due date →
   hold-from-service decision awaiting approval → approve with comment →
   case/dashboard reflect approval → audit explorer shows the chain →
   technical inspector shows extraction + rule trace. Must run in CI.
4. Dashboard empty/loading/error states; tablet responsiveness for
   overview per UX_SPEC.
5. Tests: component tests for tour engine + widget value rendering; the
   north-star spec; keep the whole existing suite green.

## Non-Goals
No tours for packs two/three (A7/L2). No analytics events (deferred). No
exports. No dashboard editor.

## Acceptance Criteria
- Live: overview shows real non-zero values after the tour journey;
  hypothetical impact card visibly badged; tour completable start→finish
  by keyboard.
- North-star Playwright green locally and in CI.
- `pnpm lint && typecheck && test && build` pass; screenshots of all three
  lens dashboards + two tour steps in the PR.

## Required Tests
Per requirement 5.

## Required Commands
`docker compose up -d && pnpm db:migrate && pnpm demo:seed --pack asset-reliability && pnpm lint && pnpm typecheck && pnpm test && pnpm build && (cd apps/web && pnpm test:e2e)`

## Documentation
`apps/web/README.md`; `docs/DEMO_SCRIPT.md` updated only if reality forced
a step change (note why).

## Handoff
`docs/agent-runs/OIW-602.md` per template. PR titled
`OIW-602: dashboard wiring, guided tour and north-star e2e`.
