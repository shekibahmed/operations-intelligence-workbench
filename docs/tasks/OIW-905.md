# Task OIW-905: Public README, Architecture Documentation and Walkthroughs

## Owner
Claude Code (Sonnet, m900x account)

## Branch
`agent/claude/OIW-905-public-docs`

## Worktree
`../oiw-quality`

## Objective
The repository's public face for M3 (PRD §37): a README that answers the
ten questions in order, an architecture document + diagram that matches
the merged system, and the three-audience walkthroughs (PRD §38) — all
truthful to what exists, nothing aspirational presented as done.

## Inputs
- `AGENTS.md`, PRD §37/§38/§36, `docs/ARCHITECTURE.md`,
  `docs/DOMAIN_MODEL.md`, ADRs 001–012, `docs/PACK_AUTHORING.md`,
  `docs/SECURITY.md`, `docs/EVALUATION.md`, `docs/DEPLOYMENT.md`,
  `docs/DEMO_SCRIPT.md`, `SESSION.md` (what is merged; tracked debt —
  which must be stated honestly, e.g. PDF text-layer fixtures)

## Owned Paths
- `README.md` (replace placeholder), `docs/ARCHITECTURE.md` (refresh to
  merged reality incl. contract versions v1.0→v1.5 and the executor
  seam), `docs/diagrams/` (Mermaid sources for system flow + lifecycle),
  `docs/walkthroughs/{leadership,operations,technical}.md`,
  `docs/CONTRIBUTING.md`, `.github/ISSUE_TEMPLATE/` (bug, pack
  proposal, question), `docs/agent-runs/OIW-905.md`

## Prohibited Paths
- All code, `scenario-packs/`, `SESSION.md`, `docs/tasks/`, `docs/PRD.md`.

## Functional Requirements
1. README per PRD §37 order + recommended opening; quick start that
   actually works from a clean clone (verify every command you print);
   demo commands; links to security/evaluation/pack-authoring/contributing;
   roadmap (Wave 5 remaining items) and a "professional adaptation" link
   placeholder pointing at `/adapt` (destination decided by owner).
2. Architecture doc: package boundary map, system flow (Mermaid),
   lifecycle diagram, contracts evolution table, deterministic-demo
   mechanism (A1), governance guarantees with test pointers.
3. Walkthroughs per PRD §38 title patterns, each grounded in one pack's
   real storyline with screenshots from `apps/web/e2e/screenshots/`.
4. Honesty pass: every claim cross-checked against merged code/tests;
   known limitations section mirrors SESSION.md tracked debt.

## Non-Goals
No launch posts/outreach copy (owner-authored). No code changes.

## Acceptance Criteria
- README quick start verified by executing it in a fresh clone; all
  internal links resolve; `pnpm lint` (markdown untouched by lint but
  repo gates must stay green); PR includes rendered-preview screenshots.

## Required Commands
`pnpm lint && pnpm typecheck && pnpm test`

## Handoff
`docs/agent-runs/OIW-905.md` per template. PR titled
`OIW-905: public README, architecture documentation and walkthroughs`.
