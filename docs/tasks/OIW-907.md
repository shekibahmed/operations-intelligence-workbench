# Task OIW-907: Public Funnel Polish — Visual Identity, Demo Experience and Multi-instance Backend

## Owner
ZCode (lead thread, owner-delegated overnight run)

## Branch
`agent/zcode/OIW-907-public-funnel-polish`

## Worktree
Primary checkout (no separate worktree; lead-run task).

## Objective
Raise the public-facing quality of the repository before outreach begins:
give the product a real visual identity (typography, tokens, landing page,
scenario selector, adapt funnel, workspace shell, dashboards), regenerate
every README screenshot from the redesigned UI, and close two tracked
backend risks (process-local rate-limit store, hardcoded DB pool) plus the
deployment path (Docker/standalone, security headers, favicon/social cards).
The demo must showcase everything the platform does — the landing page now
enumerates the full capability surface and links each entry point.

## Inputs
- Owner instruction (2026-09-12, session goal): make the repo much better —
  design, backend, code quality; demo should cover all the organisation's
  work; push when happy.
- `docs/UX_SPEC.md` (structure/a11y constraints), `docs/PRD.md` §13/§16/§20,
  `docs/SECURITY.md` §3.9, `docs/DEPLOYMENT.md` §5, `SESSION.md` tracked debt,
  existing e2e/a11y contracts in `apps/web/e2e/`.

## Owned Paths
- `apps/web/**` (pages, components, e2e helpers), `apps/web/public/**`
  (new), vendored font + licence under `apps/web/src/app/fonts/`,
  `packages/persistence/**` (rate-limit store, pool config, schema +
  migration 0003), `pnpm-lock.yaml` (integrator: workspace dep addition),
  `Dockerfile` (new), `.dockerignore` (new), `docker-compose.yml` (web
  service), `docs/screenshots/**` (regenerated), `docs/SECURITY.md`
  (§3.9 store option), `docs/DEPLOYMENT.md` (§5 rewrite),
  `docs/ARCHITECTURE.md` (stale CI line), `docs/tasks/OIW-907.md`,
  `docs/agent-runs/OIW-907.md`, `SESSION.md` (lead bookkeeping),
  `.gitignore` (local test-screenshot dir).

## Prohibited Paths
- `docs/PRD.md`, `docs/PLAN_AMENDMENTS.md`, `packages/contracts/**`
  (no contract change), `scenario-packs/**`, `db/migrations` history before
  0003.

## Functional Requirements
1. Design system: semantic token refresh + Inter variable font (vendored,
   OFL) + elevation/radius/typography scale; `prefers-reduced-motion` and
   axe serious/critical gates keep passing.
2. Marketing surface: landing page (nav, hero with real product frame,
   pipeline explainer, lenses, capability grid, packs preview, quick start,
   Kaamchor CTA, footer), scenario selector, guided start, adapt page.
3. Product surface: TopBar with brand mark, segmented lens switcher,
   iconified primary nav, session chip, synthetic-data band, restyled
   widgets (stat, severity, trend, activity, SLA, list, tables, badges).
4. SEO/social: metadata template, OG/Twitter card (build-time generated),
   favicon/icon, `NEXT_PUBLIC_SITE_URL` override.
5. Backend: `OIW_RATE_LIMIT_STORE=postgres` shared atomic token-bucket store
   with concurrency test; `DATABASE_POOL_MAX` env; opt-in
   `NEXT_OUTPUT=standalone`; multi-stage Dockerfile + compose `web`
   service; security headers + `poweredByHeader: false`.
6. Screenshots: regenerate all seven README frames from the redesigned UI
   via the real guided-tour journey (production server).
7. All quality gates pass; full Playwright suite passes.

## Non-Goals
- No hosted deployment (owner decision still open), no email/webhook CTA
  sinks (owner decision), no dark mode (not specified), no new widget kinds
  or contract changes, no changes to the three packs.
