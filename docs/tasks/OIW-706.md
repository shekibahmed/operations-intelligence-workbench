# Task OIW-706: Registry Underscore-Exclusion Convention

## Owner
Codex (high reasoning)

## Branch
`agent/codex/OIW-706-registry-exclusion`

## Worktree
`../oiw-packs`

## Objective
Implement OIW-705's requested resolution, option 1 (authoritative request:
`docs/agent-runs/OIW-705.md` on branch agent/claude/OIW-705-pack-authoring):
`buildPackRegistry` skips directory names starting with `_` during
enumeration, reported as skipped-with-reason (consistent with the existing
narrative-only skip behaviour) — so `scenario-packs/_template/` never
appears in the app/demo. WITH one lead addition: `scripts/validate-packs.ts`
must still fully validate `_`-prefixed packs (labelled as templates) so the
scaffold cannot rot unvalidated in CI. Tiny bounded task.

## Inputs
- `AGENTS.md`, that OIW-705 agent-run (read via
  `git show origin/agent/claude/OIW-705-pack-authoring:docs/agent-runs/OIW-705.md`),
  `packages/scenario-sdk/src/registry.ts`, `scripts/validate-packs.ts`,
  the registry tests (incl. the exactly-three-packs assertion)

## Owned Paths
- `packages/scenario-sdk/` (registry + tests)
- `scripts/validate-packs.ts`
- `docs/agent-runs/OIW-706.md`; lockfile only if regeneration needed

## Prohibited Paths
- `packages/contracts/`, `scenario-packs/*` (the `_template` content is on
  the OIW-705 branch, not main — your change must work on main WITHOUT it
  present, tested via a test-fixture `_`-dir), `apps/`, engines,
  migrations, `SESSION.md`, `docs/tasks/`. Zero breaking changes; the
  existing exactly-three-packs registry assertion stays meaningful
  (update it to also assert the skip behaviour, don't delete it).

## Functional Requirements
1. Registry enumeration skips `_`-prefixed directories with
   `status: "skipped"` + reason mentioning the template convention;
   documented in the SDK README.
2. `validate:packs` validates `_`-prefixed directories too, reporting them
   under a distinct "template" label; exit-code semantics unchanged
   (an invalid template fails the script).
3. Tests: test-fixture `_`-dir proves registry skip + validator inclusion;
   existing tests pass (three-packs assertion extended, not weakened).
4. `packages/application/test/common-lifecycle.integration.test.ts`: only
   if its `buildPackRegistry` usage needs the documented one-line
   adjustment per the OIW-705 handoff — nothing more.

## Acceptance Criteria
- `pnpm lint && typecheck && test && build && validate:packs && architecture:check` pass on THIS branch (no _template present); CI green.

## Required Commands
`pnpm lint && pnpm typecheck && pnpm test && pnpm build && pnpm validate:packs && pnpm architecture:check`

## Handoff
`docs/agent-runs/OIW-706.md` per template. PR titled
`OIW-706: registry underscore-exclusion convention`.
